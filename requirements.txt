# Requirements for DFS Education
# Install: pip install -r requirements.txt

Django==6.0.2
Pillow==12.1.1
python-dotenv==1.2.1
sqlparse==0.5.5
tzdata==2025.3
polib==1.2.0           # Compile locale .po -> .mo (manage.py compile_translations)

# Production
whitenoise==6.8.2      # Serve compressed static files from the app
gunicorn==23.0.0       # WSGI server (non-cPanel deployments)

# Uncomment for MySQL (production):
# mysqlclient==2.2.7
